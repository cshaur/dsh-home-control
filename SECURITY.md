\# Security Policy



Report vulnerabilities by email (do NOT open a public issue).



\- Never commit real tokens to Git; use `${ENV}` placeholders.

\- All traffic stays on your LAN or your configured cloud endpoints.

\- Scene/mode "off" operations require two-phase approval.

